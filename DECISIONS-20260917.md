# 결정 기록 — 2026-09-17 (무인 실행)

대표님 지시: *"예전 버전의 이미지들은 넣지말고 모두 새롭게 개편하자 / 완벽하게
만들고 보고해 / 3시간동안 쉬지말고 검수 작업을 반복해서 결과물 가지고와"*

판정자 표기: `클로드` = 내가 단독 판단 · `클로드+코덱스` = 2차 검토를 거침.
아래 전부 **클로드 단독**이다 — 되돌리기가 한 줄이거나, 기존 규칙·검증이 이미
답을 정해 둔 선택이라 점수가 4점에 못 미쳤다.

---

## 1. 옛 카드(v4) 경로를 완전히 걷어냈다

- **결정**: 카드 아홉 종류 전부 v5로만 만든다. 게이트에 걸리면 **그 틱에 안
  보내고 다음 틱에 다시 만든다.** 옛 디자인으로 대체하지 않는다.
- **근거**: 대표님 지시 원문(*"예전 버전의 이미지들은 넣지말고"*). 그리고 이
  폴백은 2026-09-08에 실제로 사고를 냈다 — 검증 1,450건이 전부 통과하는 동안
  채널에는 옛 디자인이 나갔고, 그것을 잡은 것은 감시가 아니라 대표님 눈이었다.
- **같이 고친 것**: 폴백이 사라지면서 운영 알림 문구가 거짓이 됐다.
  `"새 디자인 대신 옛 카드가 나갔습니다"` → `"카드를 못 만들어 이번에 거름"`.
  주석 7곳·검증 문구 4곳도 같이 고쳤다. **틀린 안내문은 없는 안내문보다 나쁘다.**
- **되돌리는 법**: `git revert` 한 번. 다만 옛 카드 코드 자체는 이미 없으므로
  되돌려도 옛 디자인이 돌아오지는 않는다.

## 2. 분석글을 '리그 묶음'에서 '경기마다 한 장'으로 바꿨다

- **결정**: `analysis_batches`(리그당 한 장에 여러 경기)를 버리고 경기마다 한 장.
- **근거**: 대표님 설계가 *"모든 경기당 한개 앵커 … 경기분석글 먼저 앵커안에
  발송"*이다. 묶음글은 앵커 하나에 못 넣는다 — 여러 경기가 한 글에 섞여 있으니
  어느 앵커의 댓글로 갈지 정할 수가 없다.
- **위험과 대응**: 경기별로 쪼개면 하루 최대 62장이 된다. 토론방 짝을 못 찾으면
  그게 전부 채널로 쏟아진다 — 채널을 조용하게 만들려던 변경이 정반대가 된다.
  → 아래 3번.
- **되돌리는 법**: `pipeline.py`의 ANALYSIS 큐 블록을 되돌린다.

## 3. 무거운 콘텐츠는 토론방 자리가 생길 때까지 기다린다 — **단, 앵커가 있을 때만**

- **결정**: 분석·라인업은 토론방 짝을 못 찾으면 이번 틱을 거른다
  (`WAIT_FOR_THREAD_TYPES`). 킥오프·득점 속보는 기다리지 않는다.
- **근거**: 속보는 창이 좁다(킥오프 29분·득점 30분). 기다리면 그냥 사라진다 —
  늦은 속보는 쓸모없지만 안 나간 속보는 더 쓸모없다.
- **스스로 잡은 결함**: 기다림의 전제는 *집이 곧 생긴다*는 것인데, **앵커가
  아예 안 나간 경기**는 집이 생길 일이 없다. 그러면 그 경기 분석은 채널에도
  토론방에도 안 나오고 **조용히 만료된다** — 눈에 안 보이는 손실이라 더 나쁘다.
  → `_anchor_is_up()`을 두어 **앵커가 이미 나간 경기에만** 기다림을 건다.
  앵커가 없으면 지금까지처럼 채널로 보낸다. **시끄러운 편이 없는 편보다 낫다.**
- **되돌리는 법**: `tick.py`의 `WAIT_FOR_THREAD_TYPES`를 `frozenset()`으로.

## 4. 토론방 버튼은 **앵커에만** 단다

- **결정**: 전달 지도(`disc.map`)를 그대로 돌지 않고, 대장에서 앵커로 나간 글
  번호만 골라(`_anchor_message_ids`) 그것에만 버튼을 단다.
- **근거**: 텔레그램은 채널 글을 **전부** 토론방으로 전달한다. 지도를 그대로
  돌았더니 득점 속보·순위표까지 모든 카드에 버튼이 붙었다(대표님이 발견).
- **되돌리는 법**: 그 함수가 빈 집합을 내면 버튼은 아무 데도 안 붙는다.

## 5. 버튼 주소는 그룹이 아니라 **그 앵커의 댓글창**으로

- **결정**: `t.me/<채널>/<글번호>?comment=<토론글번호>` (`comment_link`).
- **근거**: `t.me/<그룹>/<번호>`는 **그룹 자체로 들어가는 주소**다. 대표님이
  두 번 지적하셨다 — *"토론방 경기정보 보기 버튼 누르면 토론방으로 안 가고
  그룹방으로 연결되고 있어"*.
- **실측(2026-09-17 19:50 KST)**: 앵커 18건 발송 · 전달 지도 39건 ·
  버튼 9건 부착. 1048번 이후 앵커는 전부 버튼이 붙었다.
- **남은 것**: 1030~1037번 앵커 8건은 버튼이 없다. 토론방 받아오기가 고쳐지기
  **전에** 나간 글이라 전달 번호를 영영 모른다. 추측으로 채우지 않는다 —
  엉뚱한 방으로 보내는 버튼은 없는 버튼보다 나쁘다. (그 글들도 텔레그램 기본
  댓글 버튼은 그대로 있다.)

## 6. LCK를 시스템에서 삭제했다

- **결정**: `League.LCK`·`League.INTL_LOL`과 모든 표 항목을 지웠다.
  `DISABLED_LEAGUES`는 빈 집합이다 — 끄는 것이 아니라 **없애는** 것이라서.
- **근거**: 대표님 지시(*"LCK 는 시스템에서 모두 삭제처리해"*).
- **되돌리는 법**: `git revert`. 리그 열거형은 계약이 검사하므로 되돌려도
  절반만 살아나는 상태는 안 된다.

## 7. 구단 로고를 앵커에 넣되, **장식 보강기로만** 다룬다

- **결정**: 로고를 못 받으면 구단색 원판에 이름 첫 글자로 그리고 그대로 보낸다.
- **근거**: 그림 한 장 때문에 그 경기 앵커를 잃는 것이 훨씬 나쁘다.
- **스스로 잡은 결함 둘**:
  ① K리그는 우리 코드(`K35`)와 소스 코드가 달라 **전 경기 로고가 조용히 빠졌다**
     — 오류도 안 나고 회색 원판이 나간다. → 이름으로도 찾게 고쳤다.
  ② `SSG`가 두 글자로 잘려 `SS`(삼성)로 읽혔다. → 3글자까지 살리고 글자를 줄인다.
- **되돌리는 법**: `logos.LOGOS_ENABLED = False` 하나. 전 카드가 구단색 점으로.

## 8. 말투는 문장을 고치지 않고 **어미만** 바꾼다

- **결정**: 산문 생성기(700줄)를 건드리지 않고, 내보내기 직전에 어미만 존댓말로
  바꾼다(`speech.py`). **표에 없는 어미는 손대지 않는다.**
- **근거**: 한국어 어미 규칙은 겹친다 — `없다`(형용사)는 `없습니다`지만
  `4패다`(명사)는 `4패입니다`이고 둘 다 '받침+다'다. 규칙으로 뭉뚱그리면
  언젠가 `4패습니다`가 채널에 나간다. 바꾸지 않은 문장은 어색할 뿐이지만
  **잘못 바꾼 문장은 틀린 말이다.**
- **되돌리는 법**: `cards_v5.caption`에서 `speech.polite_lines` 한 줄을 뺀다.

## 9. AI 답변은 뼈대만 두고 껐다

- **결정**: `answers.AI_ENABLED = False`. 켜면 `NotImplementedError`가 난다.
- **근거**: 대표님 지시(*"봇답변까지만 구현시키고, ai 는 다음에 적용시키자,
  뼈대만 만들어두고"*). **조용히 아무 답도 안 하는 것보다 대놓고 터지는 편이**
  나중에 켤 때 안전하다.

---

## 하지 않은 것 (경계)

- **영구삭제 0건** — 지운 것은 코드 경로뿐이고 전부 git에 남아 있다.
- **외부 전송 0건** — 텔레그램으로 내가 직접 보낸 글은 없다. 채널에 나간 것은
  전부 깃허브에서 도는 시계가 보낸 것이다.
- **상시 프로세스 정지 0건** — `com.aiden.*`·`com.aibridge.*`는 건드리지 않았다.
- **드라이런은 발송 경로에 닿지 않는다** — 대장·전송기를 만들기 전에 끝난다
  (`tick.py`의 `if dry_run:` 블록이 그 앞에서 돌아간다). 확인하고 돌렸다.

---

## 10. 축구 경기에도 '경기 내용' 문단을 쓴다

- **결정**: 총평 첫 문단이 비는 종목(축구)은 **골 목록**으로 내용을 쓴다 —
  선제골 · 결승골(막판이면 막판이라고) · 완봉 · 멀티골 · 무승부의 흐름.
- **근거**: 첫 문단은 **안타·실책**으로만 썼다. 야구에만 있는 값이다. 그래서
  축구 총평은 순위·맞대결 두 문단뿐이었고, 정작 *"경기가 어떻게 흘렀나"*가
  통째로 빠져 있었다 — 대표님이 가장 먼저 지적하신 대목이다.
- **틀린 말을 막은 자리 셋** (전부 검증에 박았다):
  ① 골 목록은 완전하다는 보장이 없다 → **세어서 점수와 맞을 때만** 쓴다.
  ② 자책골은 어느 편으로 묶이는지 실측한 적이 없다 → 섞이면 침묵한다.
  ③ *"두 팀 다 앞서 봤다"*는 **세어서 확인한다.** 1-0, 1-1, 1-2, 2-2는
     홈이 한 번도 앞선 적이 없다 — 골 수만 보고 쓰면 거짓이 나간다.

## 11. 취소·연기는 '경기 결과'가 아니다

- **결정**: 취소 카드는 `🌧 경기 취소`, 연기는 `🕓 경기 연기`. 아이콘도 따로.
  시각 칸은 `시작`이 아니라 `예정`이라고 쓴다.
- **근거**: 실렌더로 확인했다 — 우천취소 경기가 **`✅ KBO 경기 결과`**라는
  머리로 나갔다. 체크 표시와 '결과'는 *치러졌다*로 읽힌다. 심각도 **높음**
  (사람이 잘못 앎). 대표님 설계에도 취소는 따로 알리는 항목이다.
- **같이 고친 것**: 그 카드는 머리말이 한 말을 본문이 두 번 되풀이하고 있었다
  (`우천취소`·`잠실`). 머리말에 이미 있는 값은 본문에서 뺀다.

## 12. 로고도 구단색도 없으면 **이름에서 색을 뽑는다**

- **결정**: 앵커의 원판 색이 비면 팀 이름 해시로 색을 만든다. 상대와 색상환
  거리가 가까우면 반대쪽으로 돌린다. 밝기는 기존 대비 보정기가 맞춘다.
- **근거**: 실렌더로 확인했다 — 로고도 구단색도 없는 리그의 앵커는
  **똑같은 회색 원 두 개**로 나간다. 그림만으로는 두 팀을 못 가린다.
- **구단색이라고 주장하지 않는다** — 같은 이름이면 늘 같은 색이 나오는
  표식일 뿐이다. `hash()`가 아니라 sha1을 쓴다(파이썬 해시는 실행마다 달라져
  같은 팀이 어제와 다른 색으로 나온다).

## 13. ★ '승률'과 '상대전적'은 리그마다 뜻이 다르다 — **조용한 사고 하나를 잡았다**

- **찾은 것**: MLB와 K리그1의 기록 **보관본이 늘 버려지고 있었다.**
  수집이 제동에 걸린 시간(30분 중 29분) 동안 그 두 리그의 **분석·순위표가
  통째로 사라진다.** 오류는 한 줄도 안 났다. 실측으로 잡았다:
  `MLB: 상대전적 0건` · `K09: 승률 불일치 소스=0.713 재계산=0.792`.
- **원인 ①** — 축구 순위표의 '승률' 칸은 **승점률** `(3승+무)/(3×경기)`다.
  야구식 `승/(승+패)`를 들이대면 전부 어긋난다. → 리그별 규칙표(`PCT_RULE`).
- **원인 ②** — MLB·K리그는 **상대전적을 애초에 안 모은다**(수집기가 `h2h={}`를
  넘긴다). 그런데 보관본 게이트는 늘 상대전적을 요구했다. *"0건은 항상 의심"*은
  **모으는 리그에서만** 참이다. → `H2H_LEAGUES = {KBO, NPB}`.
- **왜 여태 안 드러났나**: 산문 쪽은 `require_h2h=bool(rb.h2h)`(있으면 검사),
  보관본 쪽은 기본값 True(늘 요구) — **같은 자료가 한쪽에선 통과하고
  다른 쪽에선 버려졌다.** 이제 기본값이 `None`이고 **리그가 정한다.**
- **되돌리는 법**: `PCT_RULE`을 비우고 `H2H_LEAGUES`에 네 리그를 다 넣으면
  예전 동작이다. 다만 그 순간 MLB·K리그 보관본이 다시 버려진다.
- **판정자**: 클로드 단독. 실측 데이터가 답을 정해 준 자리라 해석의 여지가 없었다.

