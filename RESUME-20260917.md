# 이어달리기 — 2026-09-17 (무인 실행)

## 지금 상태 한 줄

채널 개편(v1.40)은 **코드와 검증이 다 끝났고, 아직 올리지 않았다.**
배포는 한 번에 모아서 한다 — 오늘 낮 여러 번 나눠 올리다 시계를 죽였다.

## 끝난 것

| 한 일 | 확인한 방법 |
|---|---|
| 옛 카드(v4) 경로 완전 제거 · 관련 주석·운영 문구 7곳 정정 | 정적 검사 + 검증 전량 |
| 분석글을 경기마다 한 장으로 (앵커 댓글로 들어갈 수 있게) | `verify_answers` |
| 무거운 콘텐츠는 토론방 자리를 기다림 — **앵커가 나간 경기만** | `verify_answers` 6건 |
| 토론방 버튼을 **앵커에만**, 주소는 그 앵커의 **댓글창**으로 | 실채널: 앵커 18건 중 1048 이후 전부 버튼 |
| 축구 총평에 '경기 내용' 문단 신설 (골 목록 기반) | `verify_prose` 15건 |
| 취소·연기를 '경기 결과'와 구분 (`🌧 경기 취소` / `🕓 경기 연기`) | `verify_cards` 8건 + 실렌더 |
| 로고·구단색 없을 때 이름에서 색을 뽑아 두 팀을 가름 | 실렌더로 확인 |
| **MLB·K리그 기록 보관본이 매번 버려지던 것** 수정 | `verify_records` 7건 + 실보관본 4개 되살림 |
| 워크플로 머리말의 낡은 설정값 주석 정정 | 실제 값과 대조 |

## 보류 — 올리기 전에 사람이 한 번 봐야 하는 것

**없다.** 배포 자체가 유일한 남은 한 발이다 (되돌리는 법: `git revert`).

## 안 한 것과 이유

- **1030~1037번 앵커 8건에는 토론방 버튼이 없다.** 토론방 받아오기가 고쳐지기
  *전에* 나간 글이라 전달 번호를 영영 모른다. 번호를 추측해서 채우지 않았다 —
  엉뚱한 방으로 보내는 버튼은 없는 버튼보다 나쁘다. (그 글에도 텔레그램 기본
  댓글 버튼은 그대로 있다.) 1048번 이후로는 전부 정상이다.
- **AI 답변은 껐다** — 대표님이 "뼈대만"이라고 하셨다. 켜면 즉시 터진다(일부러).
- **유료 데이터(결장·부상)** — 무료로는 어디에도 없다는 것을 확인했다. 다음 순서.

## 다시 시작할 때

1. `PYTHONPATH=. python3 g1/verify_*.py` 와 `g1/regress.py` 를 돌려 전량 통과 확인
2. 한 번에 커밋 → 푸시 (경기가 몰리는 시간은 피한다)
3. 배포 3~5분 뒤 `state/discussion.json` 의 `buttoned` 가 늘어나는지 본다
4. 채널에서 앵커의 **토론방 버튼을 실제로 눌러** 그 경기 댓글창이 열리는지 본다

## 부작용

없다. 영구삭제 0건 · 외부 전송 0건 · 상시 프로세스 정지 0건.
드라이런은 대장·전송기를 만들기 전에 끝나므로 채널에 아무것도 보내지 않는다.
