# 누드TV 발행 시스템 — 운영 가이드

> 이 문서 하나로 **켜고 · 지켜보고 · 문제가 생기면 대처**할 수 있습니다.
> 최종 갱신 2026-08-29 · 버전 v1.11c

---

## 0. 한눈에 — 이게 뭐고 어떻게 도는가

```
 ① 깨어난다        ② 모아온다           ③ 그린다        ④ 보낸다
 (시계)      →    (9개 리그 일정·결과) →  (카드 PNG)  →  (텔레그램 채널)
                        ↓                                    ↑
                   state/ 폴더에 기록  ────────────────────────┘
                   "무엇을 언제 보냈나" (이게 없으면 두 번 보냅니다)
```

**핵심 성질 세 가지만 기억하시면 됩니다.**

| 성질 | 뜻 | 왜 이렇게 만들었나 |
|---|---|---|
| **늦으면 안 보낸다** | "10분 뒤 시작" 카드가 20분 늦으면 스스로 버립니다 | 늦은 안내는 거짓말입니다 |
| **모르면 안 보낸다** | 결과가 아직 없으면 다음 차례에 다시 봅니다 | 빈 카드보다 늦는 게 낫습니다 |
| **두 번은 절대 안 보낸다** | 보낸 기록(`state/`)이 유일한 근거입니다 | 중복 발송은 되돌릴 수 없습니다 |

---

## 1. 어디서 돌리는가 — **깃허브 공개 저장소 · 무료** ✅

| 항목 | 설정 |
|---|---|
| 비용 | **0원** (공개 저장소는 실행 시간 **무제한**) |
| 결제수단 | 등록 불필요 |
| 깨어나는 주기 | **5분마다** (하루 288회) |
| 코드 | 공개 |
| 봇 토큰·채널 ID | **공개되지 않음** (아래 참조) |

**공개해도 안전하게 만든 것**

| 무엇 | 어디에 | 공개되나 |
|---|---|---|
| 봇 토큰 | 깃허브 암호 보관함(Secrets) | ❌ |
| 채널 ID | 발송 기록에 **지문**으로만 (`ch` + 12자리) | ❌ |
| 개인 chat_id | 암호 보관함 | ❌ |
| 코드 · 발송 기록 | 저장소 | ✅ (대표님 승인) |

채널 ID는 원래 발송 기록에 그대로 적혀 있었습니다. 공개 전에 **되돌릴 수 없는 지문**으로
바꿨습니다 — 같은 채널은 항상 같은 지문이라 중복 발송 방지는 그대로 작동하고,
지문만 봐서는 원래 번호를 알 수 없습니다.

**점검은 손이 아니라 스크립트가 합니다.** `python g1/verify_public.py` 를 돌리면
토큰·채널ID·개인정보·개발경로가 저장소에 섞였는지 17가지를 검사합니다.
실제로 이 스크립트가 첫 실행에서 제가 주석에 실수로 남긴 채널 ID를 잡아냈습니다.

**시작 알림도 나갑니다 — 리그마다 하루 한 통, 첫 경기 2시간 전.**

```
⚾ 오늘 KBO 5경기
> ◆ 18:00
>   키움 vs 두산 · 잠실
>   KT vs 삼성 · 대구
>   LG vs 롯데 · 사직
>   SSG vs KIA · 광주
>   NC vs 한화 · 대전
첫 경기 2시간 뒤 시작
```

**왜 경기마다 따로 안 보내나** — 그렇게 하면 실측 **하루 26번**이 나왔고, 그중 17번이
MLB의 새벽 1~3시대였습니다. 새벽에 열일곱 번 울리는 채널은 구독자가 나갑니다.
리그별 한 통으로 묶으니 **하루 4~5번**이 되고, 그 한 통에 그날 시간표가 다 들어갑니다.

남은 시간은 **보내는 순간에 계산**하므로 시계가 몇 분 늦어도 문구가 틀리지 않습니다.
경기 시작 5분 전을 넘기면 아예 보내지 않습니다(늦은 안내는 거짓말이므로).

> 💡 **알림 시점을 바꾸고 싶으시면** — 워크플로 파일의 `START_ALERT_LEAD_MINUTES` 숫자
> 하나입니다(지금 120 = 2시간 전). 말씀만 주세요.

---

## 2. 켜는 법 (A안 · 깃허브 · 약 20분)

### 2-1. 저장소 만들기 (5분)

1. **github.com** 로그인 → 우측 상단 **+** → **New repository**
2. 이름: `nudetv` (원하시는 걸로)
3. **Public**(공개) 선택 ← 이래야 실행 시간이 무제한입니다
4. **Create repository**

### 2-2. 파일 올리기 (5분)

제가 드리는 `nudetv-배포.zip` 을 풀면 이런 구조입니다.

```
contract.py            ← 전 시스템의 규칙
g1/                    ← 수집·렌더·발송
  tick.py              ← 시계 (이게 매번 실행됩니다)
  adapters/            ← 리그별 수집기
cards/v4.html          ← 카드 디자인
.github/workflows/     ← 언제 깨어날지
state/                 ← 발송 기록 (비어 있음, 자동으로 쌓입니다)
```

저장소 화면에서 **Add file → Upload files** 로 통째로 끌어다 놓고 **Commit** 하시면 됩니다.

### 2-3. 비밀값 넣기 (5분) — **대표님이 직접**

저장소 → **Settings** → 왼쪽 **Secrets and variables** → **Actions** → **New repository secret**

| 이름 | 값 | 필수 |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` | BotFather가 준 토큰 | ✅ |
| `TELEGRAM_CHAT_ID` | 테스트 채널 ID (`-100`으로 시작하는 값) | ✅ |
| `ALERT_CHAT_ID` | 대표님 개인 chat_id | 권장 |
| `FOOTBALL_DATA_TOKEN` | 유럽 6개 대회 키 | 나중에 |

> ⚠️ **이 값들은 저에게 보내지 마세요.** 이 화면에 대표님이 직접 붙여넣으시면 됩니다.
> 한 번 넣으면 다시 볼 수 없게 가려지고, 로그에도 찍히지 않습니다.

**`ALERT_CHAT_ID`를 권하는 이유** — 이걸 비워두면 오류 알림이 발행 채널로 갑니다.
채널이 막혀서 난 오류라면 **알림도 같이 막힙니다.** 개인 DM으로 받으시는 게 안전합니다.

### 2-4. 첫 실행 — 드라이런 (5분)

1. 저장소 → **Actions** 탭 → 왼쪽 **누드TV 발행 시계**
2. 우측 **Run workflow** → **드라이런**을 **true**로 둔 채 실행
3. 2~3분 뒤 로그를 열어 확인합니다

이렇게 나오면 정상입니다.

```
[틱] 2026-08-29 08:00 KST · 드라이런
  [수집] KBO 240 · MLB 95 · NPB 289 · ...
  [커버리지] 리그 9개 이상 없음
  [큐] 전체 33 · 지금 처리 1
   ▶ 08-29 07:30 morning  KBO  KBO:2026-08-29
```

**드라이런은 아무것도 보내지 않습니다.** 무엇을 보낼 계획인지만 보여줍니다.

### 2-5. 진짜 켜기

드라이런이 깨끗하면, **Run workflow → 드라이런 false** 로 한 번 돌려 채널에 카드가 오는지 확인합니다.
그다음부터는 **5분마다 자동으로** 돕니다. 아무것도 안 하셔도 됩니다.

---

## 3. 매일 무슨 일이 일어나나

| 한국시간 | 하는 일 |
|---|---|
| 07:30 | **모닝 브리핑** — 오늘 편성 카드 (리그별로 각각) |
| 첫 경기 2시간 전 | **시작 알림** — 그 리그 오늘 시간표 한 통 (하루 4~5번) |
| 경기 종료 후 | **결과 카드** — 그날 전 경기 결과 |
| 5분마다 | 수집 → 큐 확인 → 보낼 게 있으면 발송 |

하루에 채널로 나가는 메시지는 **대략 15~20건**입니다(모닝 4~5 · 시작 알림 4~5 · 결과 4~5).

**카드에 다 안 들어가는 내용은** 카드에 상위 8경기만 싣고, **전체는 붙어 있는 텍스트**에 담습니다.
텍스트는 접었다 펼치는 인용블록이라 채널 스크롤을 잡아먹지 않습니다.

---

## 4. 문제가 생기면

### 알림이 오면

봇이 이런 메시지를 보냅니다.

```
⚠️ 시계 점검 필요
> KBO: GateError 페이지 구조 변경 의심
> 묵은 '예정' 3건 예) NPB 2026-08-28
```

**대표님이 하실 일은 그 내용을 저에게 그대로 보내주시는 것뿐입니다.** 원인은 제가 찾습니다.

### 자주 나오는 알림과 뜻

| 알림 | 뜻 | 급한가 |
|---|---|---|
| `수집 실패 — LCK: ratelimited` | 위키가 잠깐 막음. 저장해둔 자료로 버팁니다 | 아니요 |
| `묵은 '예정' N건` | 소스가 결과를 아직 안 채웠습니다 | 하루 이상 계속되면 알려주세요 |
| `수집이 멈춤 — 마지막 성공 9시간 전` | 그 리그 소스가 바뀌었을 수 있습니다 | **네** |
| `시즌 중인데 경기가 0건` | 소스 구조 변경 의심 | **네** |
| `부분 발송 — 이어지는 텍스트 누락` | 사진은 나갔고 뒤 텍스트가 빠졌습니다 | **네** |

### 카드가 안 올라올 때 확인 순서

1. **Actions 탭**에 초록 체크가 있나요? → 없으면 실행 자체가 안 된 것입니다
2. 로그에 `[발송] 성공 0`이라면 → **보낼 게 없었던 것**입니다 (정상일 수 있습니다)
3. `Forbidden: bot is not a member` → 봇이 채널 관리자에서 빠졌습니다
4. 그 외 → 로그를 저에게 보내주세요

---

## 5. 절대 하지 마실 것

### ❌ `state/` 폴더를 지우지 마세요

이 폴더가 **"무엇을 언제 보냈는지"의 유일한 기록**입니다.
지우면 시스템은 오늘 것을 아직 안 보낸 줄 알고 **전부 다시 보냅니다.**
구독자에게 같은 카드가 두 번 가는 것은 되돌릴 수 없습니다.

> 저장소를 정리하고 싶으시면 `state/` 만 남기고 나머지를 정리하세요.

### ❌ 워크플로를 여러 개 동시에 돌리지 마세요

`Run workflow`를 연달아 누르면 겹쳐 돌 수 있습니다.
겹침 방지가 걸려 있지만(`concurrency`), 굳이 시험하지 마세요.

### ❌ 봇 토큰을 채팅·문서·코드에 붙여넣지 마세요

한 번이라도 노출되면 BotFather에서 **`/revoke`** 로 즉시 폐기하고 새로 발급하세요.

### ❌ 저장소가 **공개**입니다 — 아무 파일이나 올리지 마세요

지금 올라간 파일들은 전부 검사를 통과한 것들입니다. 여기에 다른 걸 추가로 올리시면
그것도 같이 공개됩니다. 특히 이런 것들은 **절대 올리지 마세요**:

- 제가 드린 **운영지도**(`operations-map.md`) — 실제 채널 ID가 적혀 있습니다
- **제어판 엑셀 시트** — 운영 정보가 들어 있습니다
- 토큰·비밀번호가 적힌 메모

파일을 추가하신 뒤에는 `python g1/verify_public.py` 를 한 번 돌려주세요.
`0 FAIL`이 아니면 무언가 새고 있는 것입니다.

---

## 6. 자주 묻는 것

**Q. 리그를 하나 끄고 싶습니다**
→ `g1/tick.py`의 `_jobs()` 안에서 그 줄을 지우면 됩니다. 말씀해주시면 제가 합니다.

**Q. 모닝 브리핑 시각을 바꾸고 싶습니다**
→ `g1/pipeline.py`의 `build_queue`에서 `hour=7, minute=30` 부분입니다. 말씀해주세요.

**Q. 시작 알림을 더 일찍/늦게 받고 싶습니다**
→ 워크플로 파일의 `START_ALERT_LEAD_MINUTES` 숫자 하나입니다 (지금 `120` = 2시간 전).
`180`으로 바꾸면 3시간 전이 됩니다.

**Q. 제대로 도는지 제가 직접 확인할 수 있나요?**
→ 됩니다. 저장소를 내려받아 아래를 실행하면 전 항목을 스스로 점검합니다(발송하지 않습니다).
```
python g1/verify_public.py    # 공개 안전 17건  ← 공개 저장소라 이게 제일 중요합니다
python g1/regress.py          # 회귀 점검 56건
python g1/verify_leagues.py   # 전 리그 수집 82건
python g1/verify_tick.py      # 시계 61건
python g1/verify_sender.py    # 발송 안전 66건
```
전부 `0 FAIL`이면 정상입니다.

**Q. 카드 디자인을 바꾸고 싶습니다**
→ `cards/v4.html` 하나만 고치면 전 리그 전 카드에 반영됩니다.

**Q. 운영 채널로 옮기려면?**
→ `TELEGRAM_CHAT_ID` 비밀값만 새 채널 ID로 바꾸면 됩니다.
**단 `state/`는 그대로 두세요** — 테스트 채널에 보낸 기록이 남아 있어야 하는 게 아니라,
멱등키에 채널 ID가 들어가 있어서 새 채널에는 자동으로 처음부터 나갑니다.

**Q. 비용이 정말 0원인가요?**
→ 공개 저장소는 Actions 실행 시간이 **무제한 무료**입니다. 5분마다 돌아도 0원입니다.
대표님이 다른 저장소에서 쓰시는 시간과도 무관합니다(공개 저장소는 한도에서 아예 빠집니다).

**Q. 나중에 비공개로 되돌리고 싶으면?**
→ 저장소 Settings → 맨 아래 **Change visibility**. 단 그러면 5분 간격이 한도를 8배 넘깁니다.
워크플로 파일의 `*/5` 줄을 지우고 그 아래 주석 처리된 정각 줄들을 살린 뒤,
`TICK_LOOKAHEAD_MINUTES`를 `60`으로 되돌리세요. 파일에 방법을 적어뒀습니다.

**Q. 며칠 꺼둬도 되나요?**
→ 됩니다. 다시 켜면 그 시점부터 돕니다. 지난 날짜 것을 몰아서 보내지는 않습니다
(결과 카드는 마감 후 6시간까지만 유효합니다).

---

## 7. 지금 상태 (2026-08-29)

| 항목 | 상태 |
|---|---|
| 리그 수집기 | **9개 동작** (KBO·MLB·NPB·KBL·V리그 남녀·K리그1·LCK·LoL 국제) |
| 유럽 6개 대회 | 코드 완성 · **키만 꽂으면 됨** |
| 카드 디자인 | v4.1 · 리그별 색·아이콘 |
| 시계 | 완성 · 검증 50건 통과 |
| 커버리지 감시 | 완성 |
| 검증 합계 | **347건 통과 / 0 실패** (공개 안전 17건 포함) |
| 실행 환경 | **깃허브 공개 저장소 · 무료 무제한 · 5분 간격** |
| 남은 것 | 저장소 만들고 비밀값 넣기 (2번 항목) · 유럽 키 (마지막) |
