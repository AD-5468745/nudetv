# 발송 대장은 **추가 전용 JSONL**이다 — 줄이 뒤에 붙기만 하고 고쳐지지 않는다.
#
# 두 실행이 같은 순간에 각자 한 줄씩 붙이면 git은 그것을 충돌로 본다.
# 그러면 대장이 저장소에 올라가지 못하고, 다음 실행은 그 회차 발송분을 모른 채
# **오늘 것을 다시 보낸다** — 되돌릴 수 없는 사고다.
# 실측 2026-09-04 06:38 UTC: 앞 실행이 끝나는 순간 새 실행이 시작해 정확히 이 일이
# 났고, 재시도 3회가 전부 "unmerged files"로 즉시 실패했다(연속 운전도 멈췄다).
#
# `union`은 양쪽에서 추가된 줄을 **둘 다 남긴다.** 추가 전용 파일에는 이것이 정답이다.
# 읽는 쪽은 멱등키로 접으므로 같은 줄이 두 번 있어도 발송은 한 번뿐이다.
# (같은 이유로 alerts.json·burst.json에는 절대 쓰지 않는다 — JSON 한 덩어리라
#  합치면 깨진다. 그 둘은 매 틱 다시 만들어지므로 워크플로가 원격 것을 받아들인다.)
state/ledger.jsonl merge=union
